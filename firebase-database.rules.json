{
  "rules": {
    "rooms": {
      "$roomCode": {
        ".read": true,
        ".write": true,
        ".validate": "newData.hasChildren(['createdAt', 'hostId', 'status', 'players'])"
      }
    },
    ".read": false,
    ".write": false
  }
}
